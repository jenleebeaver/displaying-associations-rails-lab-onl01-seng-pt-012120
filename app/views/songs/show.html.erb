
<h1><%= @song.title %></h1>

<h3><%= pluralize(@song.artist.count, 'Artist') %></h3>

  <ul>
    <% @song.artists.each do |a| %>
      <li><%= link_to a.name, artist_path(a) %></li>
    <% end %>
  </ul>
